  
import  java.util.Scanner ;

class  AreaTriangulo {
 entrada de escáner estático =  nuevo  escáner ( System . in);
    public  static  void  main ( String [] args ) { // metodo principal
      // Definir Variable y otros
    
      Sistema . fuera . println ( " Hola mundo " );
      int b; // definir la base de la variable
      int h; // definir altura variable
      int area = 0 ;
      // datos de entrada
      Sistema . fuera . println ( " Ingrese la Base " );
      b = entrada . nextInt (); // capturar datos del usuario para base
      
      Sistema . fuera . println ( " Ingrese la Altura " );
      h = entrada . nextInt (); // capturar datos del usuario para altura
      
      // proceso
      área = (b * h) / 2 ;
      // Datos de Salida
      Sistema . fuera . println ( " Area de un Triangulo es: " + area);
    }



}